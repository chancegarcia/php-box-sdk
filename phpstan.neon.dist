includes:
    - phpstan-baseline.neon

parameters:
    level: 0
    paths:
        - src
        - tests
        - bin
    excludePaths:
        - vendor
        - var
    ignoreErrors:
        -
            message: '#implicitly nullable via default value null#'
            reportUnmatched: false
